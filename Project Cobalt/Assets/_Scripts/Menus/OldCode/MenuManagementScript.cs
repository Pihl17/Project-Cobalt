using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MenuManagementScript : MonoBehaviour
{
	
	/*public MenuOption firstOption;
	MenuOption currentOption;

	public void OpenMenu() {
		SwitchOption(firstOption);
	}

	void SwitchOption(MenuOption next) {
		if (currentOption && currentOption.GetComponent<Text>())
			currentOption.GetComponent<Text>().color = Color.white;
		currentOption = next;
		if (currentOption.GetComponent<Text>())
			currentOption.GetComponent<Text>().color = Color.green;
	}


	public void Navigate(Vector2 direction) {
		MenuOption nextOption = null;
		if (direction.x > 0)
			nextOption = currentOption.GetRightNeighbour();
		else if (direction.x < 0)
			nextOption = currentOption.GetLeftNeighbour();
		else if (direction.y > 0)
			nextOption = currentOption.GetUpNeighbour();
		else if (direction.y < 0)
			nextOption = currentOption.GetDownNeighbour();
		


		if (nextOption)
			SwitchOption(nextOption);
	}

	public void SelectCurrentOption() {
		currentOption.Select();
	}*/


}
